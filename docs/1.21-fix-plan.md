# 1.21.11 驗收後修正總表

整理時間：2026-05-20

用途：

- 將 Step 26 驗收後要修正的項目集中在一份文件。
- 後續可用 `/goal` 依本文件逐項處理。
- 本文件是修正入口，不取代詳細驗收紀錄。

參考文件：

- `docs/1.16-to-1.21-function-checklist.md`
- `docs/1.21-config-code-audit.md`
- `docs/1.21-post-fix-retest.md`

## 處理原則

1. 先修小範圍且可獨立驗證的項目。
2. 每次修正後需封裝並啟動伺服器確認插件可正常啟動。
3. 若修正觸及 gameplay / scenario 邏輯，需補對應遊戲內驗證。
4. 不為了整理而過度抽象化；除非有明確共用邏輯或相容性需求。
5. 已驗收正常、且後續只改文字格式的 scenario 不整組重測。

## 目前總狀態

狀態（2026-05-21）：本文件列出的第一階段到第八階段程式修正切片皆已完成，各切片均已通過封裝與 `check/paper-1.21.11/start.bat` 啟動 smoke test，且 `docs/1.21-post-fix-retest.md` 的遊戲內回測項目已全數通過。

下一步：

- 若要收尾，先檢視 git diff，確認是否需要同步 `docs/1.16-to-1.21-function-checklist.md` 或整理提交。
- 若後續有新失敗回報，再依失敗項目新增小範圍修正切片；不要在沒有失敗證據時繼續擴大重構。

## 第一階段：權限與指令宣告一致性

目標：讓 LuckPerms 要授予的權限名稱、`plugin.yml`、`permissions.txt`、程式碼檢查字串一致。

狀態（2026-05-20）：已完成第一刀修正並通過封裝 / 啟動 smoke test。

驗證：

- `bash scripts/package-plugin-1.21.sh` 通過。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/check/paper-1.21.11" --port 25569 --skip-build` 通過。
- `check/paper-1.21.11/start.bat` 啟動到 `Done (30.765s)`，`WonderlandUHC v1.0.0-alpha-2` 正常 enable。
- 測試後已用 console `stop` 正常關閉，25567 / 25569 無殘留監聽。

### FIX-PERM-01 `/setspawn` 權限名稱不一致

現況：

- 程式碼 `UHCPermission.COMMAND_SET_SPAWN` 產生 `wonderland.uhc.command.set.spawn`。
- `plugin.yml` / `permissions.txt` 寫的是 `wonderland.uhc.command.setspawn`。

預期：

- 統一成實際要給 LuckPerms 的權限名稱。

可能涉及：

- `src/main/java/org/mcwonderland/uhc/UHCPermission.java`
- `src/main/resources/plugin.yml`
- `src/main/resources/permissions.txt`

驗證：

- 給非 OP 玩家對應權限後可使用 `/setspawn`。
- 無權限玩家仍被擋下。

修正狀態（2026-05-20）：已修正程式碼，`UHCPermission.COMMAND_SET_SPAWN` 改為檢查既有文件 / `plugin.yml` 權限 `wonderland.uhc.command.setspawn`。

### FIX-PERM-02 `/respawn` 權限宣告缺漏

現況：

- 程式碼檢查 `wonderland.uhc.command.respawn`。
- `plugin.yml` / `permissions.txt` 沒有完整列出。

預期：

- `plugin.yml`、`permissions.txt` 與程式碼一致。

可能涉及：

- `src/main/resources/plugin.yml`
- `src/main/resources/permissions.txt`

驗證：

- 有權限玩家可執行 `/respawn <player>`。
- 無權限玩家不可執行。

修正狀態（2026-05-20）：已補 `plugin.yml` 與 `permissions.txt` 宣告，並納入 `moderator` 權限樹。

### FIX-PERM-03 清理 `wonderland.uhc.command.uhc.spectoggle`

現況：

- 目前實際指令是 `/spectoggle`。
- 實際權限是 `wonderland.uhc.command.spectoggle`。
- `wonderland.uhc.command.uhc.spectoggle` 疑似舊殘留。

預期：

- 文件與 `plugin.yml` 不再誤導服主。

可能涉及：

- `src/main/resources/plugin.yml`
- `src/main/resources/permissions.txt`

驗證：

- `/spectoggle` 權限仍正常。

修正狀態（2026-05-20）：已從 `plugin.yml` 與 `permissions.txt` 移除 `wonderland.uhc.command.uhc.spectoggle`，保留實際使用的 `wonderland.uhc.command.spectoggle`。

### FIX-PERM-04 觀戰聊天 global 權限宣告

現況：

- 原程式碼有使用 `wonderland.uhc.spectator.chat.global`。
- `permissions.txt` 曾列出。
- `plugin.yml` 曾在第一階段補宣告。
- 第三階段修正觀戰聊天隔離後，此權限會讓觀戰者繞過隔離對全服廣播，與委託人需求衝突。

預期：

- 觀戰聊天不再提供全服廣播權限。
- 保留 `wonderland.uhc.seespectatorchat` 作為管理者查看觀戰聊天的權限。

可能涉及：

- `src/main/resources/plugin.yml`
- `src/main/resources/permissions.txt`
- `src/main/java/org/mcwonderland/uhc/game/player/role/spectator/SpectatorChat.java`

驗證：

- 依最終設計測觀戰聊天可見範圍。

修正狀態（2026-05-21）：第三階段已從程式碼、`plugin.yml`、`permissions.txt` 移除 `wonderland.uhc.spectator.chat.global`，避免觀戰者對遊玩中玩家全服廣播。觀戰聊天可見範圍已完成遊戲內回測。

## 第二階段：已確認 gameplay / scenario regression

### FIX-SCN-01 `ABSORPTION_LESS` 未覆蓋金頭顱

狀態（2026-05-21）：已修正程式碼並通過封裝 / 啟動 smoke test；遊戲內吃金頭顱實測已通過。

現況：

- 金蘋果、附魔金蘋果不給 absorption。
- 金頭顱仍會給 absorption。
- 與 1.16.5 行為不一致。

預期：

- 啟用 `ABSORPTION_LESS` 時，金頭顱也不給 absorption。

驗證：

- 遊戲中吃金蘋果、附魔金蘋果、金頭顱都不取得 absorption。

修正內容：

- `GoldenHeadListener` 在給金頭顱 absorption 前檢查 `ABSORPTION_LESS` 是否啟用。
- 啟用 `ABSORPTION_LESS` 時，金頭顱仍給 regeneration，但不再補 absorption。

已完成驗證：

- `bash scripts/package-plugin-1.21.sh` 通過。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/check/paper-1.21.11" --port 25569 --skip-build` 通過。
- `check/paper-1.21.11/start.bat` 啟動到 `Done (27.786s)`，`WonderlandUHC v1.0.0-alpha-2` 正常 enable。
- 測試後已用 console `stop` 正常關閉。

遊戲內回測項目：

- 遊戲內啟用 `ABSORPTION_LESS` 後，吃金頭顱不取得 absorption。

### FIX-SCN-02 `CUT_CLEAN` 銅礦未掉燒好銅錠

狀態（2026-05-21）：已修正程式碼並通過封裝 / 啟動 smoke test；遊戲內挖銅礦實測已通過。

現況：

- 銅礦未生效。
- 委託人確認動物範圍維持現狀。

預期：

- 銅礦 / raw copper 對應掉落燒好的銅錠。
- 動物不擴大範圍。

驗證：

- 啟用 `CUT_CLEAN` 後挖銅礦可取得銅錠。

修正內容：

- `OreRuleSupport.cookedOreDrop` 補上 `COPPER_ORE`、`DEEPSLATE_COPPER_ORE`、`RAW_COPPER` 對應 `COPPER_INGOT`。
- 動物掉落範圍維持現狀，不擴大。
- 補 `OreRuleSupportTest` 鎖住銅礦 / 深層銅礦 / 生銅轉銅錠規則。

已完成驗證：

- `bash scripts/package-plugin-1.21.sh` 通過。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/check/paper-1.21.11" --port 25569 --skip-build` 通過。
- `check/paper-1.21.11/start.bat` 啟動到 `Done (27.285s)`，`WonderlandUHC v1.0.0-alpha-2` 正常 enable。
- 測試後已用 console `stop` 正常關閉。

遊戲內回測項目：

- 遊戲內啟用 `CUT_CLEAN` 後，挖銅礦 / 深層銅礦可取得銅錠。

## 第三階段：聊天與觀戰隔離

### FIX-CHAT-01 `/me` / `/minecraft:me` 公開廣播繞過

狀態（2026-05-21）：已修正程式碼並通過封裝 / 啟動 smoke test；遊戲內輸入實測已通過。

現況：

- `/me`、`/minecraft:me` 可繞過隊伍聊天直接全服發話。
- `/say` 對無權限玩家無效，不列為一般玩家風險。
- `/msg`、`/w`、`/tell` 私訊類不特別防。

預期：

- `/me` 與 `/minecraft:me` 不應讓玩家繞過隊伍 / 觀戰聊天規則。

驗證：

- 無權限玩家輸入 `/me`、`/minecraft:me` 不產生全服公開訊息。

修正內容：

- `ChatListener` 攔截 `PlayerCommandPreprocessEvent`。
- 玩家輸入 `/me` 或 `/minecraft:me` 時直接取消，不輸出公開訊息。

已完成驗證：

- `bash scripts/package-plugin-1.21.sh` 通過。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/check/paper-1.21.11" --port 25569 --skip-build` 通過。
- `check/paper-1.21.11/start.bat` 啟動到 `Done (26.305s)`，`WonderlandUHC v1.0.0-alpha-2` 正常 enable。
- 測試後已用 console `stop` 正常關閉。

遊戲內回測項目：

- 遊戲內由 `_SnYe` 輸入 `/me test`、`/minecraft:me test` 不產生全服訊息。

### FIX-CHAT-02 觀戰者聊天不應被遊玩中玩家看到

狀態（2026-05-21）：已修正程式碼並通過封裝 / 啟動 smoke test；遊戲內多人可見範圍實測已通過。

現況：

- 兩版目前都能讓遊玩中玩家看到觀戰者聊天。
- 委託人要求遊玩中玩家不能看到觀戰聊天。

預期：

- 觀戰者訊息只給觀戰者與有管理權限者看。
- 遊玩中玩家不可見。

驗證：

- `_SnYe` 觀戰發話，`C6Yelan` 若仍在遊玩中不可見。
- 有管理權限者依設定可見。

修正內容：

- `SpectatorChat` 移除 `wonderland.uhc.spectator.chat.global` 全服廣播分支。
- 觀戰聊天只發送給已死亡 / 觀戰玩家，以及擁有 `wonderland.uhc.seespectatorchat` 的玩家。
- 從 `UHCPermission`、`plugin.yml`、`permissions.txt` 移除 `wonderland.uhc.spectator.chat.global`。

已完成驗證：

- 同 `FIX-CHAT-01`，封裝與啟動 smoke test 已通過。

遊戲內回測項目：

- `_SnYe` 觀戰發話時，仍在遊玩的 `C6Yelan` 不可見。
- 擁有 `wonderland.uhc.seespectatorchat` 的管理者可見。

## 第四階段：隊伍設定權限

### FIX-TEAM-01 一般玩家應可修改自己的隊伍設定

狀態（2026-05-21）：已修正 `plugin.yml` 預設權限並通過封裝 / 啟動 smoke test；遊戲內 `_SnYe` 實測已通過。

現況：

- `wonderland.uhc.team.settings.name`
- `wonderland.uhc.team.settings.character`
- `wonderland.uhc.team.settings.color`

目前預設為 `op`，導致無權限玩家不能調整自己的隊伍設定。

預期：

- 一般玩家可調整自己的隊伍名稱、字元、顏色。
- 不應因此獲得管理其他隊伍的能力。

驗證：

- `_SnYe` 無 OP 可修改自己隊伍設定。
- `_SnYe` 不能修改不屬於自己的隊伍。

修正內容：

- `/team settings` 入口本身已是 `TeamOwnerCommand`，只會開啟自己的隊伍設定。
- 將 `wonderland.uhc.team.settings.name`、`wonderland.uhc.team.settings.character`、`wonderland.uhc.team.settings.color` 的 `plugin.yml` 預設值從 `op` 改為 `true`。
- 未移除現有權限檢查，避免額外改動選單流程。

已完成驗證：

- `bash scripts/package-plugin-1.21.sh` 通過。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/check/paper-1.21.11" --port 25569 --skip-build` 通過。
- `check/paper-1.21.11/start.bat` 啟動到 `Done (27.577s)`，`WonderlandUHC v1.0.0-alpha-2` 正常 enable。
- 測試後已用 console `stop` 正常關閉。

遊戲內回測項目：

- `_SnYe` 無 OP 可修改自己隊伍名稱、徽章、顏色。

## 第五階段：文字格式與 MiniMessage

### FIX-TEXT-01 item 名稱 / lore 預設斜體

狀態（2026-05-21）：已修正程式碼並通過封裝 / 啟動 smoke test；遊戲內 GUI / item hover 實測已通過。

現況：

- 1.21.11 多處 item 名稱與 lore 顯示斜體。
- 1.16.5 為正體。

預期：

- GUI item 標題與敘述預設為正體。

驗證：

- 遊戲設定書、GUI item、scenario 說明等顯示正體。

修正內容：

- 新增 `PluginText.toItemComponent`，將 item component 未明確指定的 italic 設為 false。
- `PluginItems` 的 item 名稱與 lore 改用 `toItemComponent`。
- 金頭顱這類直接設定 display name 的地方也改用 `toItemComponent`。
- 補 `PluginTextTest` 驗證 item component 預設非斜體。

已完成驗證：

- `bash scripts/package-plugin-1.21.sh` 通過。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/check/paper-1.21.11" --port 25569 --skip-build` 通過。
- `check/paper-1.21.11/start.bat` 啟動到 `Done (24.825s)`，`WonderlandUHC v1.0.0-alpha-2` 正常 enable。
- 測試後已用 console `stop` 正常關閉。

遊戲內回測項目：

- 遊戲設定書、GUI item、scenario 說明、金頭顱名稱在遊戲內顯示為正體。

補漏修正（2026-05-21）：

- 遊戲內回測發現特殊模式文字已為正體，但 `ScenarioSettingsMenu` 重新建立 item 時只留下啟用 / 停用狀態，沒有保留 scenario icon 原本的 `Description` lore。
- 已改為沿用 `scenario.getIcon()` 的既有 display name / lore，再追加空行與啟用 / 停用狀態；不新增共用 item abstraction。

補漏驗證（2026-05-21）：

- `compileJava compileTestJava` 通過。
- `bash scripts/package-plugin-1.21.sh --skip-tests --no-clean` 通過。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/paper-1.21.11" --port 25567 --skip-build` 通過。
- `paper-1.21.11/start.bat` 啟動到 `Done (19.707s)`，`WonderlandUHC v1.0.0-alpha-2` 正常 enable。
- 因本次啟動使用非互動 stdin，無法送 console `stop`，測試後已停止對應 Windows Java 程序。
- 遊戲內重新回測：特殊模式設定頁 scenario item 可正常顯示敘述說明。

### FIX-TEXT-02 raw MiniMessage 顯示

狀態（2026-05-21）：已修正程式碼並通過封裝 / 啟動 smoke test；遊戲內顯示實測已通過。

現況：

- 模板標題顏色顯示 `<blue>...` raw 文字。
- 記分板預覽顯示 raw tag 並被壓成一行。
- 遊戲標題設定完成訊息顯示 raw MiniMessage。

預期：

- 玩家可見訊息依需求渲染 MiniMessage。
- 若是設定值本身，需明確決定顯示 raw 還是渲染後文字。

驗證：

- 模板、scoreboard preview、遊戲標題提示都符合預期。

修正內容：

- 遊戲標題在主設定 GUI 與「標題已變更為」訊息中改用格式化 placeholder 插入，避免 MiniMessage 被 escape 成 raw 文字。
- 儲存模板標題改用格式化 placeholder 插入，讓 saved game title 可渲染 MiniMessage。
- `GamePlaceholderReplacer` 的 `{title}` 改用格式化插入，讓模板 lore / 廣播設定預覽中的標題可渲染。
- scoreboard theme preview 將多行 preview 以 `\n` join 後作為格式化內容插入，避免 `List.toString()` 壓成一行與顯示 raw tag。

已完成驗證：

- `bash scripts/package-plugin-1.21.sh` 通過。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/check/paper-1.21.11" --port 25569 --skip-build` 通過。
- `check/paper-1.21.11/start.bat` 啟動到 `Done (24.693s)`，`WonderlandUHC v1.0.0-alpha-2` 正常 enable。
- 測試後已用 console `stop` 正常關閉。

遊戲內回測項目：

- 模板標題不再顯示 `<blue>...` raw 文字。
- scoreboard preview 為多行且不顯示 raw tag。
- 遊戲標題設定完成訊息渲染 MiniMessage，而非顯示 raw tag。

補漏修正（2026-05-21）：

- 遊戲內回測發現 scoreboard 顯示 `將於 00:20後傳送玩家`，與 1.16.5 的 `00:20 後` 空格不一致。
- `src/main/resources/scoreboards.yml` 已在 Default lobby、Ultra lobby、Ultra starting 三個倒數句的時間後方補空格。
- check 伺服器的舊 `plugins/WonderlandUHC/scoreboards.yml` 已刪除，重新啟動後已從新 jar 生成新版檔案。

補漏驗證（2026-05-21）：

- `bash scripts/package-plugin-1.21.sh --skip-tests --no-clean` 通過。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/check/paper-1.21.11" --port 25567 --skip-build` 通過。
- `check/paper-1.21.11/start.bat` 啟動到 `Done (17.915s)`，`WonderlandUHC v1.0.0-alpha-2` 正常 enable。
- 重新生成的 `check/paper-1.21.11/plugins/WonderlandUHC/scoreboards.yml` 已確認 `00:20` 與 `後` 對應位置有空格。
- `latest.log` 未出現 `ERROR`、`Exception`、`NoClassDefFoundError`、`NoSuchMethodError` 或 `Could not pass event`。
- 因本次啟動使用非互動 stdin，無法送 console `stop`，測試後已停止對應 Windows Java 程序。

### FIX-TEXT-03 `/uhc` 與 tutorial 連結

狀態（2026-05-21）：已修正程式碼並通過封裝 / 啟動 smoke test；遊戲內指令輸出實測已通過。

現況：

- `/uhc` 輸出仍有原作者 Discord link。
- `/uhc tutorial config` 內仍有原作者 Discord link。

預期：

- 改為插件下載連結。

驗證：

- 執行 `/uhc`、`/uhc tutorial config` 顯示正確連結。

修正內容：

- 新增 `References.DOWNLOAD_LINK`，目前指向 `https://github.com/MCWonderland/WonderlandUHC/releases`。
- `/uhc` 無參數首屏改顯示插件下載頁面。
- `/uhc tutorial config` 內原作者 Discord 社群文字改為插件下載頁面。
- 專案內指定的原作者購買連結 `https://discord.gg/pJ3RkP5` 已無 production 命中。

已完成驗證：

- `bash scripts/package-plugin-1.21.sh` 通過。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/check/paper-1.21.11" --port 25569 --skip-build` 通過。
- `check/paper-1.21.11/start.bat` 啟動到 `Done (26.760s)`，`WonderlandUHC v1.0.0-alpha-2` 正常 enable。
- 測試後已用 console `stop` 正常關閉。

遊戲內回測項目：

- 遊戲內執行 `/uhc` 顯示 `https://github.com/MCWonderland/WonderlandUHC/releases`。
- 遊戲內執行 `/uhc tutorial config`，相關段落顯示同一下載連結，且不再顯示 `discord.gg/pJ3RkP5`。

## 第六階段：GUI 導覽與輸入流程

### FIX-GUI-01 缺少返回上一頁紅石物品

狀態（2026-05-21）：已修正程式碼並通過封裝 / 啟動 smoke test；遊戲內 GUI 點擊回測已通過。

現況：

- 多個子頁 1.21.11 沒有 1.16.5 的返回上一頁紅石物品。

預期：

- 需要對齊的 GUI 補回返回上一頁按鈕。

驗證：

- 特殊模式、隊伍設定、邊界、資料版設定、生成預覽、公告等頁面可返回上一層。

已完成修正：

- `ScenarioSettingsMenu`、`TeamModeSettingsMenu`、`BorderSettingsMenu`、`TimeSettingsMenu`、`BroadcastSettingsMenu`、`CenterCleanerMenu` 右下角補上既有 `Leave` 紅石返回物品。
- 點擊返回物品會回到 `MainSettingsMenu`，未新增共用導覽框架，維持既有 `SavedSettingsMenu` 的局部模式。

補漏修正（2026-05-21）：

- 遊戲內回測發現 `ScoreboardSettingsMenu`（資料版設定）也缺少返回上一頁紅石按鈕。
- 已在 `ScoreboardSettingsMenu` 右下角補上既有 `Leave` 紅石返回物品，點擊回到 `MainSettingsMenu`。

補漏驗證（2026-05-21）：

- `compileJava compileTestJava` 通過。
- `bash scripts/package-plugin-1.21.sh --skip-tests --no-clean` 通過。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/paper-1.21.11" --port 25567 --skip-build` 通過。
- `paper-1.21.11/start.bat` 啟動到 `Done (19.707s)`，`WonderlandUHC v1.0.0-alpha-2` 正常 enable。
- 因本次啟動使用非互動 stdin，無法送 console `stop`，測試後已停止對應 Windows Java 程序。
- 遊戲內重新回測：資料版設定頁右下角返回紅石按鈕正常，且可回到主設定頁。

已完成驗證：

- `bash scripts/package-plugin-1.21.sh`：`BUILD SUCCESSFUL`。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/check/paper-1.21.11" --port 25569 --skip-build`：部署完成。
- 透過 `check/paper-1.21.11/start.bat` 啟動測試伺服器：`WonderlandUHC v1.0.0-alpha-2` 正常啟用，伺服器出現 `Done (24.417s)!`。
- 測試後已用 console `stop` 正常關閉。

遊戲內回測項目：

- 遊戲內確認特殊模式、隊伍設定、邊界、時機點、資料版設定、公告遊戲、生成預覽頁面的紅石返回物品存在且可回到上一層。

### FIX-GUI-02 聊天輸入完成後不返回原 GUI

狀態（2026-05-21）：已修正 host 設定輸入流程並通過封裝 / 啟動 smoke test；遊戲內輸入回測已通過。

現況：

- 多個聊天室輸入項目完成後，1.21.11 不會重開原 GUI。

預期：

- 輸入完成後回到對應設定 GUI。

驗證：

- 玩家上限、邊界、時機點、背包物品、禁止物品等輸入完成後返回原 GUI。

已完成修正：

- `MainSettingsMenu`：玩家上限、遊戲標題輸入完成後重開主設定 GUI。
- `MainSettingsMenu`：初始背包、練習模式背包、死亡掉落物、禁止物品編輯在 `/finish` 完成後重開主設定 GUI。
- `BorderSettingsMenu`：邊界大小、地獄邊界大小、最終邊界大小、邊界收縮速度、收縮耗時計算器輸入完成後重開邊界設定 GUI。
- `TimeSettingsMenu`：各時間點輸入完成後重開時間點設定 GUI。

已完成驗證：

- `bash scripts/package-plugin-1.21.sh`：`BUILD SUCCESSFUL`。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/check/paper-1.21.11" --port 25569 --skip-build`：部署完成。
- 透過 `check/paper-1.21.11/start.bat` 啟動測試伺服器：`WonderlandUHC v1.0.0-alpha-2` 正常啟用，伺服器出現 `Done (25.832s)!`。
- 測試後已用 console `stop` 正常關閉。

補漏修正（2026-05-21）：

- 遊戲內回測發現遊戲標題輸入完成後，執行者會同時收到 `Chat.broadcast(...)` 與 `Chat.sendConversing(...)` 的同一則 `標題已變更為...` 訊息。
- `MainSettingsMenu` 已改為只直接回覆輸入者一次，保留標題保存、MiniMessage 渲染、完成音效與回主設定 GUI，不再額外 broadcast 同一則完成訊息。

補漏驗證（2026-05-21）：

- `compileJava compileTestJava` 通過。
- `bash scripts/package-plugin-1.21.sh --skip-tests --no-clean` 通過。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/check/paper-1.21.11" --port 25567 --skip-build` 通過。
- `check/paper-1.21.11/start.bat` 啟動到 `Done (21.006s)`，`WonderlandUHC v1.0.0-alpha-2` 正常 enable。
- `latest.log` 未出現 `ERROR`、`Exception`、`NoClassDefFoundError`、`NoSuchMethodError` 或 `Could not pass event`。
- 因本次啟動使用非互動 stdin，無法送 console `stop`，測試後已停止對應 Windows Java 程序。

遊戲內回測項目：

- 遊戲內確認玩家上限、遊戲標題、邊界數值、時機點輸入完成後會回到來源 GUI。
- 遊戲內確認初始背包、練習模式背包、死亡掉落物、禁止物品編輯 `/finish` 後會回到主設定 GUI。
- 遊戲內確認遊戲標題輸入完成訊息只顯示一次。

## 第七階段：音效與後台紀錄

### FIX-SOUND-01 音效與 1.16.5 不一致

狀態（2026-05-21）：已補設定 GUI 主要音效與第二批分散音效，並通過封裝 / 啟動 smoke test；實際遊戲內音效回測已通過。

現況：

- 多個 GUI 點擊、切換、輸入完成音效與 1.16.5 不一致或缺少。

預期：

- 能合理對齊 1.16.5 體感。

驗證：

- 以 checklist 已列出的音效差異逐項抽測。

已完成修正：

- 玩家上限、遊戲標題、邊界數值、時機點等聊天輸入開始與完成時會播放設定變更音效。
- 初始背包、練習模式背包、死亡掉落物、禁止物品等背包編輯開始時會播放設定變更音效；完成時沿用既有 `Inventory_Edited` 音效。
- 蘋果掉落機率、初始經驗等級、地獄開關、終界珍珠傷害、同隊傷害、邊界模式切換時會播放設定變更音效。
- 記分板更新頻率、愛心顏色、sidebar theme 選擇會播放設定變更音效。
- 公告遊戲按鈕進入 Discord 公告流程時會播放設定變更音效。
- 模板列表的儲存、載入、覆蓋、刪除操作會播放設定變更音效。
- 為避免既有伺服器的 `plugins/WonderlandUHC/sounds.yml` 缺少新 key 導致插件無法啟動，本輪未新增必填 `sounds.yml` 欄位，而是沿用既有 `Host.Scenario_Toggled` 音效。

已完成驗證：

- 初次嘗試新增 `Host.Setting_Changed` 時，啟動測試發現既有測試服設定缺少 `Host.SETTING_CHANGED` 會導致 `Missing configuration value` 並停用插件；已改回相容既有設定的做法。
- `rg -n "SETTING_CHANGED|Setting_Changed" src/main/java src/main/resources/sounds.yml`：無結果。
- `bash scripts/package-plugin-1.21.sh`：`BUILD SUCCESSFUL`。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/check/paper-1.21.11" --port 25569 --skip-build`：部署完成。
- 透過 `check/paper-1.21.11/start.bat` 啟動測試伺服器：`WonderlandUHC v1.0.0-alpha-2` 正常啟用，伺服器出現 `Done (29.297s)!`。
- 測試後已用 console `stop` 正常關閉。
- 補第二批分散音效後再次執行 `bash scripts/package-plugin-1.21.sh`：`BUILD SUCCESSFUL`。
- 再次部署並透過 `check/paper-1.21.11/start.bat` 啟動測試伺服器：`WonderlandUHC v1.0.0-alpha-2` 正常啟用，伺服器出現 `Done (28.051s)!`。
- 測試後已用 console `stop` 正常關閉。

遊戲內回測項目：

- 遊戲內確認玩家上限、遊戲標題、邊界數值、時機點的輸入開始 / 完成有音效。
- 遊戲內確認背包編輯開始與 `/finish` 完成有音效。
- 遊戲內確認蘋果掉落機率、初始經驗等級、地獄開關、終界珍珠傷害、同隊傷害、邊界模式切換有音效。
- 遊戲內確認記分板設定、公告遊戲、模板列表操作有音效。

### FIX-LOG-01 後台紀錄缺失與 `System.out`

狀態（2026-05-21）：已移除聊天事件殘留 `System.out` debug 輸出，並補回白名單、隊伍、邊界、地獄切換後台紀錄；已通過封裝 / 啟動 smoke test，實際操作 console 回測已通過。

現況：

- 聊天輸入先前會印 `[STDOUT]` 並觸發 Paper nag；2026-05-20 已移除 `TutorialListener` 內的殘留 `System.out.println("Hello")`。
- 白名單、隊伍、邊界、地獄等切換先前缺少 1.16.5 後台紀錄；2026-05-20 已在對應切換點用 `PluginConsole.log` 同步輸出原本的聊天廣播訊息。

預期：

- 移除 `System.out`。
- 需保留的後台紀錄改用 plugin logger。

驗證：

- 操作對應設定時 console 沒有 Paper nag。
- 後台紀錄符合決定保留的行為。

已完成修正：

- `TutorialListener`：移除殘留 `System.out.println("Hello")`。
- `MainSettingsMenu`：白名單、地獄開關除了原本聊天廣播外，會同步寫入 console。
- `TeamModeSettingsMenu`：同隊傷害開關除了原本聊天廣播外，會同步寫入 console。
- `BorderSettingsMenu`：邊界模式切換除了原本聊天廣播外，會同步寫入 console。

已完成驗證：

- `rg -n "System\\.(out|err)" src/main/java`：無結果。
- `bash scripts/package-plugin-1.21.sh`：`BUILD SUCCESSFUL`。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/check/paper-1.21.11" --port 25569 --skip-build`：部署完成。
- 透過 `check/paper-1.21.11/start.bat` 啟動測試伺服器：`WonderlandUHC v1.0.0-alpha-2` 正常啟用，伺服器出現 `Done (25.947s)!`。
- 測試後已用 console `stop` 正常關閉。
- 補後台紀錄後再次執行 `bash scripts/package-plugin-1.21.sh`：`BUILD SUCCESSFUL`。
- 再次部署並透過 `check/paper-1.21.11/start.bat` 啟動測試伺服器：`WonderlandUHC v1.0.0-alpha-2` 正常啟用，伺服器出現 `Done (27.582s)!`。
- 測試後已用 console `stop` 正常關閉。

遊戲內回測項目：

- 遊戲內輸入玩家上限、遊戲標題、邊界、時機點等聊天內容時，console 不再出現 `[WonderlandUHC] [STDOUT] Hello` 或 Paper nag。
- 遊戲內切換白名單、同隊傷害、邊界模式、地獄開關時，console 會出現對應 `[WonderlandUHC] [白名單] ...`、`[隊伍] ...`、`[邊界] ...`、`[地獄] ...` 紀錄。

## 第八階段：Whitelist

### FIX-WHITELIST-01 自訂 whitelist 功能品質修正

狀態（2026-05-21）：已做最小修正並通過封裝 / 啟動 smoke test；登入訊息優先順序已完成遊戲內回測。

現況：

- `/whitelist` 是插件自訂實作，不是原版指令。
- 使用者已決定不再深入測目前行為，但功能不刪除。
- 已確認的明顯問題是設定階段白名單開啟時，非 OP 玩家會先看到 `目前白名單是開啟狀態。`，而不是設定階段應優先顯示的 `遊戲設定中，請稍做等待。`。

預期：

- 保留自訂 whitelist 功能，不重寫 `/whitelist add/remove/list/clear`。
- Preparing 階段的世界生成、設定中、滿人等登入 gate 優先於 whitelist gate。
- 白名單本身仍在準備階段後段與遊戲中階段生效。

已完成修正：

- `PreparingLoginListener` 的登入檢查順序調整為 `GeneratingChecker`、`ConfigChecker`、`FullChecker`、`WhitelistChecker`。
- 這讓設定階段的 kick reason 回到設定中訊息優先，避免 whitelist 訊息蓋掉設定階段 gate。

已完成驗證：

- `bash scripts/package-plugin-1.21.sh`：`BUILD SUCCESSFUL`。
- `bash scripts/deploy-to-windows-server.sh --server-dir "/mnt/c/Users/a0919/OneDrive/桌面/Minecraft local server/check/paper-1.21.11" --port 25569 --skip-build`：部署完成。
- 透過 `check/paper-1.21.11/start.bat` 啟動測試伺服器：`WonderlandUHC v1.0.0-alpha-2` 正常啟用，伺服器出現 `Done (36.513s)!`。
- 測試後已用 console `stop` 正常關閉。

驗證：

- 遊戲內確認設定階段白名單開啟時，`_SnYe` 加入會優先顯示 `遊戲設定中，請稍做等待。`。
- 跑圖完成 / 允許加入階段再確認白名單開啟時，未列入名單且無 bypass 權限的玩家仍會被 whitelist 擋下。

## 暫不處理 / 已接受差異

- 聊天輸入逾時取消不保留。
- 聊天輸入前大量空白洗版不保留。
- fallback lobby 紅石粉不保留。
- CutClean 動物範圍維持現狀。
- `/msg`、`/w`、`/tell` 私訊類不用特別防。
- 已驗收正常、且後續只改文字格式的 scenario 不重測。
