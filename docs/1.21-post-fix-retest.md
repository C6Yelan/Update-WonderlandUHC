# 1.21.11 修正後回測清單

整理時間：2026-05-21

用途：

- 集中追蹤 `docs/1.21-fix-plan.md` 已完成程式修正後的遊戲內驗證結果。
- 每項回測只判斷「修正後行為是否符合預期」，不重新展開已驗收正常的功能。
- 若回測失敗，回到 `docs/1.21-fix-plan.md` 補新的修正切片。

目前 gate：

- 程式修正切片已完成，且已通過封裝 / 啟動 smoke test。
- 2026-05-21 遊戲內回測已完成，本文件已無未勾選項目。
- 若後續出現新失敗回報，再回到 `docs/1.21-fix-plan.md` 新增小範圍修正切片。

測試帳號：

- `C6Yelan`：OP / host 視角。
- `_SnYe`：一般玩家視角。

狀態標記：

- `[ ]`：尚未回測。
- `[x]`：回測通過。
- `[!]`：回測失敗，需回到 `docs/1.21-fix-plan.md` 新增修正切片。
- `[-]`：本輪無法測或已決定不測，需在該項後方補原因。

回報格式：

- 可以直接回報「章節名稱 + 項目摘要 + 結果」。
- 範例：`文字格式 / 遊戲設定書名稱與 lore：正常。`
- 若失敗，保留實際訊息、截圖路徑或 console 原文。
- 若一批項目都正常，可回報「GUI 導覽與輸入全正常」，再由文件逐項標記。

建議測試順序：

1. 先測不需要開局的設定階段項目：權限、文字格式、GUI 導覽、音效、console。
2. 再測需要 `_SnYe` 加入或退出的項目：whitelist、隊伍設定、`/me`、觀戰聊天。
3. 最後測需要開局或 scenario 的項目：`ABSORPTION_LESS`、`CUT_CLEAN`。

## 權限與登入

- [x] `C6Yelan` 給 `_SnYe` `wonderland.uhc.command.setspawn` 後，`_SnYe` 可執行 `/setspawn`；撤權後不可執行。2026-05-21 回測：LuckPerms 設為 true 後 `_SnYe` 執行 `/setspawn` 顯示 `成功儲存大廳重生點。`；unset 後顯示 `抱歉，你沒有權限使用此功能。`
- [x] 有 `wonderland.uhc.command.respawn` 權限的玩家可執行 `/respawn <player>`；無權限玩家不可執行。2026-05-21 回測：LuckPerms 設為 true 後 `_SnYe` 執行 `/respawn` 可進入插件邏輯，曾顯示 `此玩家目前正在遊玩中，因此不需要復活。`，並可顯示 `_SnYe 使用了神之掌復活了 C6Yelan。`；unset 後顯示 `抱歉，你沒有權限使用此功能。`
- [x] `/spectoggle` 使用 `wonderland.uhc.command.spectoggle` 正常，沒有依賴舊的 `wonderland.uhc.command.uhc.spectoggle`。2026-05-21 回測：`wonderland.uhc.command.spectoggle` 預設 `true`；遊玩中玩家執行顯示 `此功能只限定觀戰使用。`，觀戰狀態執行顯示 `[觀戰系統] 已切換觀戰模式，請使用 /spectoggle 可再切換成另一種。`
- [x] 設定階段白名單開啟時，`_SnYe` 加入會優先顯示 `遊戲設定中，請稍做等待。`。2026-05-21 回測：白名單開啟且處於 host 設定期時，`_SnYe` 加入顯示 `遊戲設定中，請稍做等待。`
- [x] 跑圖完成 / 允許加入階段白名單開啟時，未列入名單且無 bypass 權限的 `_SnYe` 仍會被 whitelist 擋下。2026-05-21 回測：`_SnYe` 加入時顯示 `目前白名單是開啟狀態。`

## Gameplay / Scenario

- [x] 啟用 `ABSORPTION_LESS` 後，吃金蘋果、附魔金蘋果、金頭顱都不取得 absorption。2026-05-21 回測正常。
- [x] 啟用 `CUT_CLEAN` 後，挖銅礦 / 深層銅礦可取得銅錠。2026-05-21 回測正常。

## 聊天與觀戰

- [x] `_SnYe` 輸入 `/me test` 不產生全服公開訊息。2026-05-21 回測：功能可正常攔截；暫不額外隱藏指令提示，保留現狀。
- [x] `_SnYe` 輸入 `/minecraft:me test` 不產生全服公開訊息。2026-05-21 回測：功能可正常攔截；暫不額外隱藏指令提示，保留現狀。
- [x] `_SnYe` 觀戰發話時，仍在遊玩的 `C6Yelan` 不可見。2026-05-21 回測：管理員可見觀戰訊息，deop 後無法看見。
- [x] 擁有 `wonderland.uhc.seespectatorchat` 的管理者可看到觀戰聊天。2026-05-21 回測：管理員可看到觀戰訊息。

## 隊伍設定

- [x] `_SnYe` 無 OP 可修改自己的隊伍名稱。2026-05-21 回測：正常。
- [x] `_SnYe` 無 OP 可修改自己的隊伍徽章。2026-05-21 回測：正常。
- [x] `_SnYe` 無 OP 可修改自己的隊伍顏色。2026-05-21 回測：正常。
- [x] `_SnYe` 不能修改不屬於自己的隊伍。2026-05-21 回測：`_SnYe` 作為非隊長執行 `/team settings` 時顯示 `此功能只有隊長才能使用。`

## 文字格式

- [x] 遊戲設定書名稱與 lore 顯示為正體。2026-05-21 回測：正常。
- [x] GUI item 名稱與 lore 顯示為正體。2026-05-21 回測：正常。
- [x] scenario 說明文字顯示為正體，且有敘述說明。2026-05-21 回測：文字已為正體，但特殊模式設定頁缺少敘述說明；已補 `ScenarioSettingsMenu` 保留 scenario icon 原本 description lore，重新回測正常。
- [x] 金頭顱名稱顯示為正體。2026-05-21 回測：正常。
- [x] 模板標題不再顯示 `<blue>...` raw 文字。2026-05-21 回測：使用 `<blue>測試標題</blue>` 作為遊戲標題儲存模板後，模板標題正常渲染。
- [x] scoreboard preview 為多行，且不顯示 raw MiniMessage tag。2026-05-21 回測：已確認多行顯示，且沒有 raw MiniMessage tag。
- [x] scoreboard 傳送倒數文字在時間與 `後` 之間有空格。2026-05-21 補漏修正：`scoreboards.yml` 已將 `{teleport_in}` / `{start_in}` 後方補空格，check 伺服器舊 `scoreboards.yml` 已刪除並重新生成新版檔案；遊戲內 sidebar 回測已顯示修正版。
- [x] 遊戲標題設定完成訊息會渲染 MiniMessage，而不是顯示 raw tag。2026-05-21 回測：輸入 `<blue>測試標題</blue>` 後顯示 `標題已變更為: 測試標題。`，顏色正常渲染。
- [x] `/uhc` 顯示 `https://github.com/MCWonderland/WonderlandUHC/releases`，且不顯示 `discord.gg/pJ3RkP5`。2026-05-21 回測：顯示插件下載頁面為 GitHub releases。
- [x] `/uhc tutorial config` 顯示 `https://github.com/MCWonderland/WonderlandUHC/releases`，且不顯示 `discord.gg/pJ3RkP5`。2026-05-21 回測：顯示專案資訊連結為 GitHub releases。

## GUI 導覽與輸入

- [x] 特殊模式頁面有紅石返回物品，且可回到主設定頁。2026-05-21 回測：有紅石返回按鈕，點擊會返回主設定頁。
- [x] 隊伍設定頁面有紅石返回物品，且可回到主設定頁。2026-05-21 回測：有紅石返回按鈕，點擊會返回主設定頁。
- [x] 邊界設定頁面有紅石返回物品，且可回到主設定頁。2026-05-21 回測：有紅石返回按鈕，點擊會返回主設定頁。
- [x] 時機點設定頁面有紅石返回物品，且可回到主設定頁。2026-05-21 回測：有紅石返回按鈕，點擊會返回主設定頁。
- [x] 資料版設定頁面有紅石返回物品，且可回到主設定頁。2026-05-21 回測：缺少返回上一頁紅石按鈕；已補 `ScoreboardSettingsMenu` 右下角返回按鈕，重新回測正常。
- [x] 公告遊戲頁面有紅石返回物品，且可回到主設定頁。2026-05-21 回測：有紅石返回按鈕，點擊會返回主設定頁。
- [x] 生成預覽頁面有紅石返回物品，且可回到主設定頁。2026-05-21 回測：有紅石返回按鈕，點擊會返回主設定頁。
- [x] 玩家上限輸入完成後會回到主設定 GUI。2026-05-21 回測：正常。
- [x] 遊戲標題輸入完成後會回到主設定 GUI。2026-05-21 回測：正常。
- [x] 遊戲標題輸入完成訊息只顯示一次。2026-05-21 回測：`標題已變更為: YEE。` 原本會顯示兩次；已修正同時 broadcast 與直接回覆造成的重複送訊息，重新回測只顯示一次。
- [x] 邊界數值輸入完成後會回到邊界設定 GUI。2026-05-21 回測：正常。
- [x] 時機點輸入完成後會回到時機點設定 GUI。2026-05-21 回測：正常。
- [x] 初始背包 `/finish` 後會回到主設定 GUI。2026-05-21 回測：正常。
- [x] 練習模式背包 `/finish` 後會回到主設定 GUI。2026-05-21 回測：正常。
- [x] 死亡掉落物 `/finish` 後會回到主設定 GUI。2026-05-21 回測：正常。
- [x] 禁止物品 `/finish` 後會回到主設定 GUI。2026-05-21 回測：正常。

## 音效

- [x] 玩家上限輸入開始 / 完成有音效。2026-05-21 回測：正常。
- [x] 遊戲標題輸入開始 / 完成有音效。2026-05-21 回測：正常。
- [x] 邊界數值輸入開始 / 完成有音效。2026-05-21 回測：正常。
- [x] 時機點輸入開始 / 完成有音效。2026-05-21 回測：正常。
- [x] 背包編輯開始與 `/finish` 完成有音效。2026-05-21 回測：正常。
- [x] 蘋果掉落機率切換有音效。2026-05-21 回測：正常。
- [x] 初始經驗等級切換有音效。2026-05-21 回測：正常。
- [x] 地獄開關切換有音效。2026-05-21 回測：正常。
- [x] 終界珍珠傷害切換有音效。2026-05-21 回測：正常。
- [x] 同隊傷害切換有音效。2026-05-21 回測：正常。
- [x] 邊界模式切換有音效。2026-05-21 回測：正常。
- [x] 記分板設定有音效。2026-05-21 回測：正常。
- [x] 公告遊戲按鈕有音效。2026-05-21 回測：正常。
- [x] 模板列表儲存、載入、覆蓋、刪除有音效。2026-05-21 回測：正常。

## Console 紀錄

- [x] 玩家上限、遊戲標題、邊界、時機點等聊天輸入不再出現 `[WonderlandUHC] [STDOUT] Hello`。2026-05-21 回測：未出現。
- [x] 玩家上限、遊戲標題、邊界、時機點等聊天輸入不再出現 Paper `System.out/err.print` nag。2026-05-21 回測：未出現。
- [x] 切換白名單時 console 會出現對應 `[WonderlandUHC] [白名單] ...` 紀錄。2026-05-21 回測：開啟 / 關閉皆正常輸出。
- [x] 切換同隊傷害時 console 會出現對應 `[WonderlandUHC] [隊伍] ...` 紀錄。2026-05-21 回測：開啟 / 關閉皆正常輸出。
- [x] 切換邊界模式時 console 會出現對應 `[WonderlandUHC] [邊界] ...` 紀錄。2026-05-21 回測：Moving / Timer 皆正常輸出。
- [x] 切換地獄開關時 console 會出現對應 `[WonderlandUHC] [地獄] ...` 紀錄。2026-05-21 回測：開啟 / 關閉皆正常輸出。
